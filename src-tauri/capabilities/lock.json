{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lock",
  "description": "Capability for the lock screen window",
  "windows": ["lock"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
